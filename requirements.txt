# Phase 0: Minimal dependencies for dataset validation
# macOS Apple Silicon compatible, no ML frameworks

# Core dependencies
zarr>=2.14.0,<3.0.0
numpy>=1.24.0,<2.0.0

# Phase 2: Kafka and schema dependencies
pydantic>=2.0.0,<3.0.0
kafka-python>=2.0.0,<3.0.0

